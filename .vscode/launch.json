{
    "configurations": [
        {
            "name": "Debug (Attach)",
            "type": "cppvsdbg",
            "request": "attach",
            "processId": "${command:pickProcess}"
        },
    ]
}
